* Find max *

Description:
	You have to write a function to find the max value in an array.


Authorized function:
	--

Prototype:
	int	find_max(const int *nbs, const size_t size);

Example:
	find_max({1, 3, 5, 2}, 4) == 5
