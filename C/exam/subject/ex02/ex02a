
Description:
	You have to write a function to find a substring "needle" in a string "haystack"
	ft_strstr should have the exactly same behavior than strstr C standard function as define in manual.


Authorized function:
	--

Prototype:
     char *ft_strstr(const char *haystack, const char *needle);

Example:
	ft_strstr("bonjour", "jour") == "jour"
	ft_strstr("bonjour", "jor") == NULL
